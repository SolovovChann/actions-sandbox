name: Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    uses: ./.github/workflows/test.yml
    steps:
      - name: install build
        run: python -m pip install -U build
      - name: build module
        run: python -m build
